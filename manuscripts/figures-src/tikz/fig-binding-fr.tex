\documentclass[border=1pt]{standalone}
\input{inspect_style.tex}

% Fig12 (binding-fr) page-readability bump — local overrides only.
% Shared house defaults stay 8/9/7; this dense 33-row panel needs larger
% type when included at \linewidth on elsarticle preprint.
% Keep axis width near the prior 4.85in so \linewidth does not shrink type;
% grow height instead. CM discrete sizes only (no half-pt substitution).
\renewcommand{\figfont}{\fontsize{12}{14}\selectfont}
\renewcommand{\axislabelfont}{\fontsize{12}{14}\selectfont}
\renewcommand{\tickfont}{\fontsize{10}{12}\selectfont}

\definecolor{C_BASE}{HTML}{D55E00}
\definecolor{C_GATE}{HTML}{009E73}

% gate, baseline, row, cell, benchmark tag, deferral percent
\newcommand{\BindingRow}[6]{%
  \draw[gray!70, line width=1.35pt] (axis cs:#1,#3) -- (axis cs:#2,#3);%
  \fill[C_BASE] (axis cs:#2,#3) circle (2.8pt);%
  \fill[C_GATE] (axis cs:#1,#3) circle (2.8pt);%
  \node[anchor=east, font=\figfont, text=black] at (axis cs:-0.02,#3) {\strut\detokenize{#4} [#5]};%
  \node[anchor=west, font=\figfont, text=black] at (axis cs:1.005,#3) {#6\%};%
}

\begin{document}
\begin{tikzpicture}[font=\figfont]
  \begin{axis}[
    inspect default,
    % Taller canvas so larger fonts do not crush the 33-row panel.
    % Width held near prior 4.85in (slightly tighter) so paper \linewidth
    % does not shrink the larger type back down.
    width=4.70in,
    height=9.50in,
    xmin=0, xmax=1.0,
    ymin=-0.6, ymax=32.8,
    xlabel={realized false-reject rate},
    ytick=\empty,
    axis lines*=left,
    clip=false,
  ]
    \draw[black, dashed, line width=1.15pt] (axis cs:0.05,-0.6) -- (axis cs:0.05,32.8);
    \node[anchor=south west, font=\figfont, text=black] at (axis cs:0.05,32.55) {target 0.05};

    \input{../out/binding-fr-rows.tex}

    \node[anchor=south east, font=\figfont, text=black] at (axis cs:1.0,-0.4) {
      \begin{tabular}{l}
        \textcolor{C_BASE}{$\bullet$} naive fixed threshold (B1) \\
        \textcolor{C_GATE}{$\bullet$} certified gate
      \end{tabular}
    };
    \node[anchor=south, font=\figfont, text=black] at (axis cs:1.005,32.3) {gate deferral};
  \end{axis}
\end{tikzpicture}
\end{document}
