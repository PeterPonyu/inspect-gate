% fig-opcost: deployment economics (2x2)
% Data: R/opcost_*.csv → out/opcost-panel-[a-d].tex
%
% Layout (2026-08-09):
%   - Equal panel heights; compact row gutter (no above-plot multi-row legends
%     eating the a/b–c/d gap).
%   - Titles near the top spine; a/b legends one-row above (SE); c legend
%     inside NW (log headroom).
%   - Panel a group names inside, above the x-spine.
%   - Panel b: ymin=0.1 so high-vol floor clears the spine (break-even at
%     10^0 is a y-tick only — no full-width guide on the floor); labels above
%     markers.
\documentclass[border=2pt]{standalone}
\input{inspect_style.tex}

\definecolor{cGate}{HTML}{0072B2}
\definecolor{cBase}{HTML}{D55E00}

\newcommand{\opcostlabel}[2]{%
  \node[anchor=south east, font=\panellabelfont, inner sep=0.5pt]
    at ([xshift=-16pt, yshift=5pt] #1.north west) {\textbf{#2}};%
}

\begin{document}
\begin{tikzpicture}[font=\figfont]

\pgfplotsset{
  opcost/title near/.style={
    title style={at={(0.5,1.02)}, anchor=south, font=\titlefont},
  },
  % Title @1.02; legend higher @1.24 SE so the two never share a baseline.
  opcost/legend above se/.style={
    legend style={
      at={(1.0,1.24)}, anchor=south east,
      font=\tickfont, draw=none, fill=none, inner sep=0.8pt,
      /tikz/every even column/.append style={column sep=4pt},
    },
  },
}

% Panel a: total cost grid
\begin{axis}[
  inspect default,
  opcost/title near,
  opcost/legend above se,
  name=axa, width=2.42in, height=1.70in, at={(0.50in,1.88in)},
  ymode=log, ymin=800, ymax=250000,
  ylabel={total cost (USD/hr, log)},
  xtick={0,1,2,3,4,5,6,7,8},
  xticklabels={low,mid,high,low,mid,high,low,mid,high},
  x tick label style={font=\tickfont, rotate=45, anchor=north east},
  ybar=0.8pt, bar width=4.5pt,
  enlarge x limits=0.06,
  legend columns=2,
  title={total hourly cost},
]
\input{../out/opcost-panel-a.tex}
\node[font=\tickfont, anchor=south] at (axis cs:1, 950) {MPDD};
\node[font=\tickfont, anchor=south] at (axis cs:4, 950) {VisA};
\node[font=\tickfont, anchor=south] at (axis cs:7, 950) {MVTec-AD};
\end{axis}
\opcostlabel{axa}{a}

% Panel b: review-cost headroom
\begin{axis}[
  inspect default,
  opcost/title near,
  opcost/legend above se,
  name=axb, width=2.42in, height=1.70in, at={(3.26in,1.88in)},
  ymode=log, ymin=0.1, ymax=4000,
  ylabel={review-cost headroom (x, log)},
  xtick={0,1,2},
  xticklabels={low-vol, mid-vol, high-vol},
  x tick label style={font=\tickfont},
  legend columns=3,
  title={review-cost headroom},
  clip=false,
]
\input{../out/opcost-panel-b.tex}
\end{axis}
% Scenario subtitles under the compact tick labels (one line, clears gutter).
\path (axb.south west) -- (axb.south east)
  node[pos=0.16, below=9pt, font=\fontsize{6}{7}\selectfont, text=black!70] {precision}
  node[pos=0.50, below=9pt, font=\fontsize{6}{7}\selectfont, text=black!70] {general}
  node[pos=0.84, below=9pt, font=\fontsize{6}{7}\selectfont, text=black!70] {commodity};
\opcostlabel{axb}{b}

% Panel c: four-method mid-vol
\begin{axis}[
  inspect default,
  opcost/title near,
  name=axc, width=2.42in, height=1.70in, at={(0.50in,0.04in)},
  ymode=log, ymin=40, ymax=80000,
  xmin=-0.55, xmax=2.55,
  ylabel={USD/hr (log, mid-vol.)},
  xtick={0,1,2},
  xticklabels={MVTec AD, MPDD, VisA},
  x tick label style={font=\tickfont},
  ybar=0pt, bar width=3.2pt,
  bar shift auto=false,
  % One-row legend above (not 2x2 in the row gutter / not over bars).
  legend style={at={(0.5,1.14)}, anchor=south, legend columns=4, font=\tickfont,
    draw=none, fill=none, inner sep=0.6pt,
    /tikz/every even column/.append style={column sep=3pt}},
  title={four-method mid-vol.\ cost},
  clip=true,
]
\input{../out/opcost-panel-c.tex}
\end{axis}
\opcostlabel{axc}{c}

% Panel d: routing latency on a μs log axis.
\begin{axis}[
  inspect default,
  opcost/title near,
  name=axd, width=2.42in, height=1.70in, at={(3.26in,0.04in)},
  ymode=log, ymin=1, ymax=100000,
  ytick={1,10,100,1000,10000},
  ylabel={$\mu$s per image (lower=faster)},
  xtick={0,1,2},
  xticklabels={Backbone\\(b=1), Backbone\\(b=32), Gate\\routing},
  x tick label style={font=\tickfont, align=center},
  ybar=0pt, bar width=12pt,
  bar shift auto=false,
  enlarge x limits=0.22,
  title={routing latency},
  clip=false,
]
\input{../out/opcost-panel-d.tex}
\end{axis}
\opcostlabel{axd}{d}

\end{tikzpicture}
\end{document}
