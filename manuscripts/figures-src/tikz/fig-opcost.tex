% fig-opcost: deployment economics (2x2)
% Data: R/opcost_*.csv → out/opcost-panel-[a-d].tex
%
% Layout (2026-08-11 panel-QA):
%   - Titles alone just above the axis spine (no per-panel legends).
%   - Mid-strip L|R: methods (a,c) under A; benchmarks (b) under B.
%     No (d) legend — Backbone/Gate named on x-ticks.
%   - Panel b: color-coded value ROW under each scenario (not jittered labels).
%   - scale only axis → predictable column walls; wide gutter clears D ylabel.
%   - Extra inter-row gap + modest height bump for mid legend + b value row.
%   - Local +1pt font bump (this figure only).
\documentclass[border=2pt]{standalone}
\input{inspect_style.tex}
\usetikzlibrary{calc}

\definecolor{cGate}{HTML}{0072B2}
\definecolor{cBase}{HTML}{D55E00}

% Local +1pt class bump vs house defaults (8/9/7 → 9/10/8).
\renewcommand{\figfont}{\fontsize{9}{10.5}\selectfont}
\renewcommand{\axislabelfont}{\fontsize{10}{11.5}\selectfont}
\renewcommand{\titlefont}{\fontsize{10}{11.5}\selectfont}
\renewcommand{\tickfont}{\fontsize{8}{9.5}\selectfont}
\renewcommand{\panellabelfont}{\fontsize{10}{11.5}\selectfont\bfseries}
% Compact group headers for the mid-strip (one step below tickfont).
\newcommand{\leghdrfont}{\fontsize{7}{8}\selectfont}

% Panel letter via house \panellabelshift → bold black.
% Top row (a/b): raised park clears long rotated ylabels.
% Bottom row (c/d): lower park so letters sit with C/D titles, not in the
% mid-strip next to the A|B legends.
\newcommand{\opcostlabel}[2]{\panellabelshift{#1}{#2}{-52pt}{32pt}}
\newcommand{\opcostlabelBD}[2]{\panellabelshift{#1}{#2}{-44pt}{32pt}}
\newcommand{\opcostlabelCD}[2]{\panellabelshift{#1}{#2}{-52pt}{12pt}}
\newcommand{\opcostlabelD}[2]{\panellabelshift{#1}{#2}{-44pt}{12pt}}

\begin{document}
\begin{tikzpicture}[font=\figfont]
\input{../out/opcost-panel-b-values.tex}

\pgfplotsset{
  opcost/title near/.style={
    title style={
      at={(0.5,1.0)}, anchor=south, yshift=6pt,
      font=\titlefont, text=black,
    },
  },
  % Plot-box geometry: `at`/`width` are the axis box (labels hang outside).
  opcost/panel box/.style={
    scale only axis,
  },
}

% Geometry (2026-08-11, scale only axis):
%   plot box 2.35×1.70in
%   left  col a|c: at x=1.15in → east=3.50in  (ylabels hang left of 1.15)
%   right col b|d: at x=4.55in → east=6.90in  (gutter 1.05in clears D ylabel)
%   c/d at y=0.10 → north=1.80; a/b at y=3.65 → mid gap 1.85in
%   mid legends @ 78pt below a/b.south (under A|B; clears b value rows + c/d)

% Panel a: total cost grid
\begin{axis}[
  inspect default,
  opcost/panel box,
  opcost/title near,
  name=axa, width=2.35in, height=1.70in, at={(1.15in,3.65in)},
  ymode=log, ymin=800, ymax=250000,
  ylabel={total cost (USD/hr, log)},
  xtick={0,1,2,3,4,5,6,7,8},
  xticklabels={low,mid,high,low,mid,high,low,mid,high},
  x tick label style={font=\tickfont, rotate=45, anchor=north east, text=black},
  ybar=0.8pt, bar width=4.5pt,
  enlarge x limits=0.06,
  title={total hourly cost},
]
\input{../out/opcost-panel-a.tex}
\node[font=\tickfont, text=black, anchor=south] at (axis cs:1, 90000) {MPDD};
\node[font=\tickfont, text=black, anchor=south] at (axis cs:4, 90000) {VisA};
\node[font=\tickfont, text=black, anchor=south] at (axis cs:7, 90000) {MVTec-AD};
\end{axis}
\opcostlabel{axa}{a}

% Panel b: review-cost headroom
\begin{axis}[
  inspect default,
  opcost/panel box,
  opcost/title near,
  name=axb, width=2.35in, height=1.70in, at={(4.55in,3.65in)},
  ymode=log, ymin=0.1, ymax=6000,
  ylabel={review-cost headroom (x, log)},
  xtick={0,1,2},
  xticklabels={low-vol, mid-vol, high-vol},
  % Extra tick-label drop so value rows sit cleanly under (not through) ticks.
  x tick label style={font=\tickfont, text=black, yshift=-1pt},
  title={review-cost headroom},
  clip=false,
]
\input{../out/opcost-panel-b.tex}
\end{axis}
% Color-coded value ROW under each scenario (VisA / MVTec-AD / MPDD) + tag.
% below=30pt (was 9pt): clears ~7pt tick labels with ~12pt air gap.
\path (axb.south west) -- (axb.south east)
  node[pos=0.16, below=30pt, font=\tickfont, align=center, inner sep=0.5pt]
    {\opcostBValColZero\\[1.5pt]\textcolor{black}{precision}}
  node[pos=0.50, below=30pt, font=\tickfont, align=center, inner sep=0.5pt]
    {\opcostBValColOne\\[1.5pt]\textcolor{black}{general}}
  node[pos=0.84, below=30pt, font=\tickfont, align=center, inner sep=0.5pt]
    {\opcostBValColTwo\\[1.5pt]\textcolor{black}{commodity}};
\opcostlabelBD{axb}{b}

% Panel c: four-method mid-vol
\begin{axis}[
  inspect default,
  opcost/panel box,
  opcost/title near,
  name=axc, width=2.35in, height=1.70in, at={(1.15in,0.10in)},
  ymode=log, ymin=40, ymax=400000,
  xmin=-0.55, xmax=2.55,
  ylabel={USD/hr (log, mid-vol.)},
  xtick={0,1,2},
  xticklabels={MVTec AD, MPDD, VisA},
  x tick label style={font=\tickfont, text=black},
  ybar=0pt, bar width=3.2pt,
  bar shift auto=false,
  title={four-method mid-vol.\ cost},
  clip=true,
]
\input{../out/opcost-panel-c.tex}
\end{axis}
\opcostlabelCD{axc}{c}

% Panel d: routing latency
\begin{axis}[
  inspect default,
  opcost/panel box,
  opcost/title near,
  name=axd, width=2.35in, height=1.70in, at={(4.55in,0.10in)},
  ymode=log, ymin=1, ymax=1200000,
  ytick={1,10,100,1000,10000,100000},
  ylabel={$\mu$s per image (lower=faster)},
  xtick={0,1,2},
  xticklabels={Backbone\\(b=1), Backbone\\(b=32), Gate\\routing},
  x tick label style={font=\tickfont, align=center, text=black},
  ybar=0pt, bar width=12pt,
  bar shift auto=false,
  enlarge x limits=0.22,
  title={routing latency},
  clip=false,
]
\input{../out/opcost-panel-d.tex}
\end{axis}
\opcostlabelD{axd}{d}

% ── Mid-strip legends: L under A (methods a,c) | R under B (benchmarks b) ──
% Park just under panel-b value rows (not on C/D titles / panel letters).
% Panel (d): omit legend — x-ticks already name Backbone / Gate routing.
% Explicit south-offsets keep L/R on one baseline while clearing b's tags.
\coordinate (midL) at ([yshift=-78pt] axa.south);
\coordinate (midR) at ([yshift=-78pt] axb.south);
\node[anchor=north, font=\tickfont, text=black, align=center, inner sep=1pt]
  at (midL) {%
  {\leghdrfont methods (a,c)}\\[2pt]
  \textcolor{cGate}{\rule{1.1ex}{1.1ex}}~gate\quad
  \textcolor{cBase}{\rule{1.1ex}{1.1ex}}~CRC\quad
  \textcolor{cDefer}{\rule{1.1ex}{1.1ex}}~B1 (no defer)\quad
  \textcolor{cAccept}{\rule{1.1ex}{1.1ex}}~All-human%
};
\node[anchor=north, font=\tickfont, text=black, align=center, inner sep=1pt]
  at (midR) {%
  {\leghdrfont benchmarks (b)}\\[2pt]
  \textcolor{cMPDD}{$\circ$}~MPDD\quad
  \textcolor{cVisA}{$\square$}~VisA\quad
  \textcolor{cMVTecAD}{$\triangle$}~MVTec-AD%
};

\end{tikzpicture}
\end{document}
